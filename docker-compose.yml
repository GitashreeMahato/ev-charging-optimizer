

services:
  postgres:
    image: postgres:16
    container_name: ev_charging_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Admin1234!
      POSTGRES_DB: evchargingdb
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: